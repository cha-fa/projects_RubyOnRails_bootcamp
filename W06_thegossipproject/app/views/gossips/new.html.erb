<div class="all">
	<h1>Créer un nouveau potin</h1>
<br>
<p>Remplis le formulaire ci-dessous pour publier un nouveau potin !</p>

<%= form_tag url_for(action: 'create'), method: "post" do %>
	  <% if @gossip.errors.any? %>

	  <div class="alert alert-danger" role="alert">
  <p>Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    <% @gossip.errors.full_messages.each do |message| %>
      <li><%= message %></li>
        </div>
    <% end %>
    <p><strong> Réessaye en respectant bien les conditions ;)</strong></p>
  <% end %>


<div class="form-group" >
  <label class="col-form-label" for="inputDefault">Titre du potin</label>
  <input type="text" class="form-control" placeholder="Titre" id="inputDefault" name="title">
</div>

<div class="form-group">
  <label for="exampleTextarea">Contenu du potin</label>
  <textarea class="form-control" id="exampleTextarea" rows="6" name="content"></textarea>
</div>

    <button type="submit" class="btn btn-primary">Publiez le potin</button>


<% end %>


 


</div>